---
interface Props {
    items: string[];
}

const { items } = Astro.props;
---
<ul class="list-disc pl-5 space-y-2">
    {items.map((item) => (
        <li>
            {item}
        </li>
    ))}
</ul>